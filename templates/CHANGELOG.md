<!--
Template for a plugin CHANGELOG. Copy to plugins/<plugin>/CHANGELOG.md.
Delete this comment block once done. See plugins/sdd-kit/CHANGELOG.md for a
worked example. Format is Keep a Changelog; versions match plugin.json's
`version` field exactly, bumped manually — this repo doesn't use git tags.
-->
# Changelog

All notable changes to the `<plugin-name>` plugin are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## <X.Y.Z>

- <What changed, in English, one bullet per notable addition/fix/removal.
  Link to the relevant ADR if the change codifies a decision, e.g.
  "Rationale recorded in [ADR 0001](docs/adr/0001-slug.md).">
