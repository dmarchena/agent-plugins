<!--
Template for a slash command. Copy to
plugins/<plugin>/commands/<command-name>.md and fill in every section.
Delete this comment block once done. See plugins/sdd-kit/commands/*.md for
worked examples — that plugin's commands are thin shortcuts into a skill;
yours doesn't have to be, but keep it short either way.
-->
---
description: <One line: what invoking this command does, in English.>
argument-hint: "[<what the free-text argument after the command means>]"
---
<The command body. For a thin shortcut into a skill, this is one line:
"Invoke the skill `<plugin>:<skill-name>` with these arguments: $ARGUMENTS".
For a command with its own logic, write the full instructions here instead.>
