{
  "spec-writer": [
    "Lite** (recommend this by default)",
    "Reserve this for cross-team",
    "don't improvise a new one",
    "Depende de:",
    "[auto]` (mechanically checkable) or `[manual]`",
    "Every THEN must name a concrete observable",
    "works correctly",
    "Know when to stop.",
    "Don't assemble the spec until the end.",
    "must be **self-contained**",
    "stay lean (~120 lines as",
    "Default to multiple choice, with one recommendation.",
    "Before moving on, settle two one-liners",
    "R-E2E"
  ],
  "plan-writer": [
    "does NOT generate one (that's spec-writer)",
    "does **not** execute any task and does **not** edit",
    "no best-effort",
    "plan-tools.mjs inspect-spec",
    "Exit code ≠ 0** → STOP",
    "Do not write any plan, not even a partial one.",
    "exactly **one verifiable deliverable**",
    "split them into **separate tasks**. Never fold",
    "The resulting graph must be acyclic",
    "most-austere-that-qualifies rule",
    "→ `haiku`; bounded",
    "duplicating it here just drifts out of sync",
    "don't invent dependencies that aren't",
    "Code and test-file names",
    "hard gate, not a nice-to-have",
    "silently drops a requirement or AC",
    "the plan is immutable once the exec stage starts",
    "execution_plan.json.tmp",
    "plan-tools.mjs check-plan",
    "Do not re-interview the user"
  ],
  "plan-executor": [
    "single TDD executor",
    "no reviewer tokens.",
    "exec-tools.mjs init SPECDIR",
    "PLAN_INVALIDO",
    "Do **not** create a branch, state, or launch any subagent",
    "(R1.S2 / AC1)",
    "exec-tools.mjs next SPECDIR",
    "ready now (≤3)",
    "never one per turn",
    "MUST require the strict TDD cycle",
    "Evidence of red BEFORE",
    "test_contract: null",
    "cite those scenario/AC IDs",
    "It resolves **no** open decisions",
    "exec-tools.mjs complete SPECDIR",
    "so it arrives as one argv token",
    "sin evidencia de rojo",
    "only trusts a green it can reproduce",
    "reason: \"no-red\"",
    "**Do not** count this as a failed",
    "Never mark a task done yourself or with git directly",
    "never edit it by hand, and never edit",
    "byte-identical to plan-writer's output",
    "A half-done task never appears as `done`.",
    "**exactly once**: re-delegate to the same subagent",
    "exec-tools.mjs block SPECDIR",
    "marks the task `blocked` and its transitive dependents `skipped`",
    "exceed **2×** the estimate",
    "Only resume on their say-so.",
    "re-runs the test command of every `done` task",
    "previously-green test now fails",
    "hand the decision to the user",
    "guarantees the **task** level (TDD tests green)",
    "commits stay on the plan branch",
    "the git module refuses",
    "happens automatically per verified task on the plan branch",
    "MUST NOT include the body of any test or implementation file",
    "MUST contain exactly: `task_id`, files touched (paths only)",
    "≤ 3 lines quoting the actual failing assertion, not just a boolean",
    "This trim applies only to the happy path",
    "MUST NOT quote the scenario/AC text verbatim into the brief",
    "bounces the task as an unresolved ambiguity instead of inventing a contract"
  ],
  "verify": [
    "AC by AC, against the plain Acceptance Criteria checklist",
    "degrading the whole checklist to manual confirmation",
    "always pass the directory.",
    "loadSpecdir(specDir)",
    "It does not re-validate the plan against the spec",
    "`loadSpecdir` throws before evaluating or",
    "confirmed **one by one, in this main conversation thread, directly with the",
    "Only an explicit \"yes, this is met\" from the user justifies",
    "either way it is **not** green",
    "MUST NOT be delegated to a subagent",
    "NOT be resolved unilaterally",
    "A subagent has no standing to give",
    "stop: that is a spec violation,",
    "it never stops at the first not-green AC",
    "never allowed to turn a green AC (or the archiving decision) red",
    "git mv SPECDIR docs/specs/archived/<slug>/",
    "this is explicitly allowed to run",
    "it refuses before running",
    "the report instead names exactly which ACs are missing",
    "drift, blocked/skipped, not finished, rejected, unanswered, or fully",
    "manualConfirmation(items)",
    "assembleReport(checklist"
  ]
}
