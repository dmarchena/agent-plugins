# Spec: fixture-wrapped-description-only (verify-tools test fixture)

## Purpose

Minimal fixture spec used by verify-tools AC-parser tests (R1.S1 / AC1). Not a
real feature.

## Acceptance Criteria

- [ ] AC1 → R1.S1 [auto] — sample automatic criterion whose description
  wraps onto a second line that does not start with a dash.
