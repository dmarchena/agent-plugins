{
  "name": "markvault",
  "version": "0.1.0",
  "description": "Deterministic, offline, ~0-token PDF-to-Markdown extraction: a local CLI selects an extraction strategy (pymupdf4llm, pdftotext+OCR, markitdown) and writes a .md next to the PDF or at an explicit path."
}
