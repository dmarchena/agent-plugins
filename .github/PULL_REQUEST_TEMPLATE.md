## Summary

<1-3 bullet points: what changed and why, not a restatement of the diff.>

## Related

<Link the spec/ADR/issue this closes or implements, if any — e.g.
`docs/specs/<slug>/spec.md`, `docs/adr/NNNN-*.md`. Delete if none.>

## Test plan

- [ ] `bash scripts/validate.sh` passes
- [ ] <Any plugin-specific test command, e.g. `node test/run.mjs` for sdd-kit>
- [ ] <Manual verification steps, if the change has no automated coverage>
